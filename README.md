# Attention OS

**个人注意力操作系统 — 对话优先的 AI 注意力助手**

Attention OS 是一款桌面端注意力管理工具。它通过截屏分析 + 本地活动信号融合，实时理解你的工作状态，并通过**悬浮对话框**以朋友般的方式与你互动——而不是弹窗打断你。

核心设计原则：**一切皆对话**。系统的所有提醒、签到、任务管理，都流经同一个悬浮对话框（`ChatOverlay`），不弹独立窗口，不打断心流。

```
📸 截图分析          🧠 状态融合          💬 对话介入
  每60秒一次    →    AI 判断是否分心   →   通过悬浮框对话
  VL 模型理解        键鼠活动加权           不弹窗、不打断
```

---

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动
python run.py
```

启动后自动打开浏览器访问 `http://127.0.0.1:5000`。

首次使用请进入 **设置 → AI 模型配置**，选择 AI 提供商并填入 API Key，保存后即可开始分析。配置持久化于本地 `data/api_settings.json`，无需 `.env` 文件。

### 启动模式

```bash
python run.py              # 完整模式（托盘图标 + Web仪表盘 + 监控）
python run.py --no-tray    # 无托盘模式（适合调试）
python run.py --web-only   # 仅启动Web服务（查看历史数据）
python run.py --cli        # 仅命令行监控（无Web）
```

### 各平台说明

#### Windows（原生）
开箱即用，截图使用 `mss`，无需额外配置。

#### macOS
首次运行需授权：
- **屏幕录制**：系统偏好设置 → 隐私与安全性 → 屏幕录制 → 勾选 Terminal/IDE
- **辅助功能**：系统偏好设置 → 隐私与安全性 → 辅助功能 → 勾选 Terminal/IDE

#### Linux（原生桌面）
需要安装系统级截图工具：
```bash
sudo apt install scrot   # X11 环境
# 或
sudo apt install grim    # Wayland 环境
```

#### WSL2（Windows Subsystem for Linux）
WSL2 下 `mss`/PIL 的 `XGetImage()` 因 WSLg 虚拟显示层限制而无法使用。
本项目已内置自动检测：**在 WSL2 下自动调用 `powershell.exe` 通过 Windows GDI+ 完成截图**，无需任何额外安装。

> 注意：请在 WSL2 终端中运行 `python run.py`，不要使用 Windows 侧的 Python 解释器。

---

## 核心功能

### 💬 ChatOverlay 对话悬浮窗

系统的唯一交互入口。所有提醒、签到、任务管理都通过这个悬浮框进行：

- 你可以随时输入任何问题或想法
- 系统分心检测触发时，自动展开悬浮窗并以对话方式提醒
- 专注模式下，悬浮窗变为极简计时器 + 快速思维捕捉
- 新消息时自动展开，无需手动点击

### 📸 截图分析 + 状态融合

- 每 60 秒自动截图，用视觉 AI 分析当前应用和内容
- 结合键鼠活动率、窗口切换频率进行多信号融合
- 连续分心超过阈值时，通过对话框发起轻推而非硬性弹窗
- 用户离开时（空闲超过 5 分钟）自动暂停截图和 LLM 分析，节省资源

### 🍅 番茄钟（`pomodoro.py`）

标准番茄工作法：工作 → 短休息 → 每 4 个循环一次长休息。

- 可在 Web 界面或对话框中启动/停止
- 支持绑定当前任务，专注模式下自动切换对话框为极简视图
- 强制休息：休息时间到自动触发全屏遮罩（`break_overlay_process.py`）
- 持久化记录每次专注时段

### ⏸ 休息提醒（`break_reminder.py`）

独立于番茄钟的工作节奏管理：

- 连续在场超过设定时长（默认 45 分钟）后提醒
- 休息超过指定时间后，通过对话框 + 提示音提醒回来工作
- 真实休息检测：离开超过 10 分钟才重置工作会话，避免短暂离开误触发

### 📋 TodoList（`todo_manager.py`）

- 支持自然语言输入，AI 自动解析标题、截止时间、优先级、标签
- 示例：「明天下午前完成项目报告，紧急」→ 自动结构化
- 在 Web 界面管理，也可通过对话框快速添加

### ⏰ 每小时签到（`hourly_checkin.py`）

- 每隔一小时通过悬浮对话框询问「你在做什么」
- 收集自我报告数据，记录工作类别和状态
- 签到数据存储于本地，可在仪表盘查看

### 📊 Web 仪表盘

- 实时状态（生产/分心/中性）
- 每小时效率分布
- 7 天趋势对比
- 活动率平滑曲线（5 点移动平均）

---

## 与同类产品的对比

| 特性 | Attention OS | RescueTime | Forest | Toggl |
|------|-------------|------------|--------|-------|
| 截屏 AI 理解上下文 | ✅ VL 模型 | ❌ | ❌ | ❌ |
| 对话式提醒（不弹窗） | ✅ ChatOverlay | ❌ | ❌ | ❌ |
| 本地部署/数据隐私 | ✅ | ❌ 云端 | ✅ | ❌ 云端 |
| 自然语言任务输入 | ✅ LLM | ❌ | ❌ | ❌ |
| 多 AI 提供商支持 | ✅ 5 家 | ❌ | ❌ | ❌ |
| 番茄钟 + 休息提醒 | ✅ | ❌ | ✅ | ❌ |

---

## 技术架构

### Multi-Agent 架构

通过统一 LLM 调用层（`llm_client.py`）驱动多个专职 Agent：

| Agent | 职责 | 模型类型 |
|-------|------|----------|
| Analyzer Agent | 截屏内容分析 | 视觉模型（VL） |
| Coach Agent | 分心提醒生成 | 文本模型 |
| Dialogue Agent | 多轮对话引擎 | 文本模型 |
| Parser Agent | 自然语言任务解析 | 文本模型 |

### 支持的 AI 提供商

| 提供商 | 说明 |
|--------|------|
| 阿里云百炼 (DashScope) | 通义千问系列，国内推荐 |
| ModelScope | 开源模型社区，支持 Qwen3 |
| DeepSeek | DeepSeek-V3 / R1 |
| OpenAI | GPT-4o 等 |
| Anthropic Claude | Claude 系列 |

默认模型（可在设置中切换）：
- 视觉模型：`Qwen/Qwen3-VL-235B-A22B-Instruct`（截屏分析）
- 文本模型：`Qwen/Qwen3-Next-80B-A3B-Instruct`（提醒生成、任务解析）

---

## 系统要求

| 项目 | 要求 |
|------|------|
| Python | >= 3.9 |
| 操作系统 | macOS / Windows / Linux |
| AI 提供商 | 任意一家，填入 API Key 即可 |

### 依赖概览

```
# 核心
requests, python-dotenv, Pillow, mss, openai

# 活动监控
pynput, psutil

# Web 服务
fastapi, uvicorn[standard]

# 系统托盘（可选）
pystray

# macOS 特定（可选，获取焦点窗口）
pyobjc-framework-Quartz, pyobjc-framework-Cocoa
```

---

## 项目结构

```
AttentionOS/
├── run.py                          # 启动入口（托盘/Web/CLI 模式）
│
├── attention/
│   ├── main.py                     # 监控主循环（每 60 秒一个周期）
│   ├── config.py                   # 全局配置
│   │
│   ├── core/                       # AI 调用层 + 数据采集层
│   │   ├── llm_client.py           # 统一 LLM 调用客户端（文本 + 视觉）
│   │   ├── llm_provider.py         # 多提供商管理（DashScope/DeepSeek/OpenAI/Claude/ModelScope）
│   │   ├── agents.py               # Multi-Agent 角色定义（Analyzer/Coach/Dialogue/Parser）
│   │   ├── api_settings.py         # API Key 持久化管理
│   │   ├── app_settings.py         # 应用级设置
│   │   ├── screenshot.py           # 屏幕截图（跨平台，含 WSL2 兼容）
│   │   ├── analyzer.py             # VL 模型截屏分析
│   │   ├── activity_monitor.py     # 键鼠活动 + 焦点窗口监控
│   │   ├── state_fusion.py         # 多信号融合引擎
│   │   ├── dialogue_agent.py       # 多轮对话引擎（会话记忆 + 上下文感知）
│   │   ├── autostart_manager.py    # 开机自启动管理
│   │   └── database.py             # 监控记录存储与查询
│   │
│   ├── features/                   # 功能模块
│   │   ├── pomodoro.py             # 番茄钟
│   │   ├── break_reminder.py       # 休息提醒
│   │   ├── todo_manager.py         # TodoList + 自然语言解析
│   │   ├── hourly_checkin.py       # 每小时签到
│   │   └── chat_logger.py          # 对话日志导出（每日 Markdown）
│   │
│   └── ui/                         # 界面层
│       ├── chat_overlay.py         # 对话悬浮窗管理器（父进程）
│       ├── chat_overlay_process.py # 对话悬浮窗 GUI（子进程）
│       ├── break_overlay_process.py# 全屏休息遮罩（子进程）
│       ├── desktop_overlay.py      # 状态追踪（不直接弹窗）
│       ├── tray_app.py             # 系统托盘图标
│       └── web_server.py           # FastAPI Web 服务 + 全部 API
│
├── static/                         # Web 前端（单页应用）
│   └── index.html
│
├── tests/                          # 测试套件
│   ├── test_hourly_checkin.py
│   ├── test_llm_failover.py
│   ├── test_llm_provider.py
│   ├── test_modelscope_api.py
│   ├── test_pomodoro_break.py
│   └── test_todo_manager.py
│
├── data/                           # 本地持久化数据（自动创建）
│   ├── work_logs.json
│   ├── todos.json
│   ├── focus_sessions.json
│   ├── checkins/
│   ├── chat_logs/                  # 每日对话 Markdown 日志
│   └── api_settings.json           # AI 提供商配置
│
└── requirements.txt
```

---

## API 参考

### 监控数据

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/status` | GET | 实时工作状态 |
| `/api/today` | GET | 今日统计数据 |
| `/api/hourly` | GET | 每小时效率分布 |
| `/api/weekly` | GET | 7 天趋势 |
| `/api/distraction` | GET | 当前分心信息 |
| `/ws` | WebSocket | 实时状态推送 |

### 番茄钟

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/pomodoro/status` | GET | 当前状态 |
| `/api/pomodoro/start` | POST | 开始（body: `{focus_task}`） |
| `/api/pomodoro/pause` | POST | 暂停 |
| `/api/pomodoro/resume` | POST | 继续 |
| `/api/pomodoro/stop` | POST | 停止 |
| `/api/pomodoro/skip-break` | POST | 跳过休息 |
| `/api/pomodoro/settings` | POST | 更新设置 |

### 任务管理

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/todos` | GET | 获取全部任务 |
| `/api/todos` | POST | 添加任务 |
| `/api/todos/smart-add` | POST | 自然语言添加 |
| `/api/todos/parse` | POST | 解析任务文本（不保存） |
| `/api/todos/{id}/toggle` | POST | 切换完成状态 |
| `/api/todos/{id}` | DELETE | 删除 |

### 休息提醒

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/break/settings` | GET | 获取设置 |
| `/api/break/settings` | POST | 更新设置 |

### 签到

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/checkin/status` | GET | 签到状态 |
| `/api/checkin/settings` | POST | 更新设置 |
| `/api/checkin/toggle` | POST | 启用/禁用签到 |

### 对话

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/chat/send` | POST | 发送消息 |
| `/api/chat/history` | GET | 获取对话历史 |
| `/api/chat/export` | POST | 导出对话日志 |
| `/api/chatlog/list` | GET | 列出每日日志文件 |
| `/api/chatlog/read/{date}` | GET | 读取指定日期日志 |

### 悬浮窗

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/overlay/status` | GET | 悬浮窗状态 |
| `/api/overlay/intervention` | POST | 手动触发分心提醒 |
| `/api/overlay/break` | POST | 触发全屏休息遮罩 |
| `/api/overlay/skip-break` | POST | 跳过当前休息 |

### 设置

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/settings/providers` | GET | 获取所有提供商配置 |
| `/api/settings/providers/{name}/key` | POST | 设置 API Key |
| `/api/settings/providers/{name}/config` | POST | 更新提供商配置 |
| `/api/settings/providers/{name}/test` | POST | 测试连接 |
| `/api/settings/providers/active` | POST | 切换激活提供商 |
| `/api/settings/autostart` | GET/POST | 开机自启动 |
| `/api/settings/theme` | GET/POST | 主题设置 |

### 备忘录

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/memo/save` | POST | 保存备忘 |
| `/api/memo/list` | GET | 列出备忘 |

---

## 版本历史

### 当前版本（功能减法重构）

大幅精简，回归核心价值。从 30+ 模块削减到核心 10 个。

**保留的核心：**
- 截图分析 + 活动监控 → 状态融合 → 分心检测
- ChatOverlay 对话悬浮窗（唯一提醒渠道）
- 番茄钟（Pomodoro）
- 休息提醒（Break Reminder）
- 待办事项（TodoList）
- 每小时签到（Hourly Check-in，简化版）
- 对话 Agent（移除已删模块的引用）

**移除的模块（~6300 行代码）：**
- 每日 Briefing / 一日回顾 / 周报
- 主动规划引擎
- 智能回归提醒（ART 模型）
- 每日开工追踪
- 插件系统（event_bus / plugin_manager / plugin_interface）
- 语音识别（SenseVoice）
- 旧版番茄钟浮窗

### v5.2
- 一切皆对话 — 核心设计哲学确立，ChatOverlay 成为唯一交互入口
- Multi-Agent 架构（Coach / Dialogue / Parser / Analyzer）
- 多 LLM 提供商支持（DashScope / DeepSeek / OpenAI / Claude / ModelScope）
- 休息结束提醒 — 休息指定分钟后通过对话+声音提醒回来工作
- 有新消息时自动展开悬浮窗

### v3.3
- 每日 Briefing、任务感知提醒、一日回顾闭环
- SenseVoice 语音输入
- 周数据洞察

### v3.0
- 番茄钟、TodoList、每小时签到、每日报告

### v2.0
- 多信号融合引擎、活动监控、Web 仪表盘

### v1.0
- 基础截屏分析 + 状态记录
