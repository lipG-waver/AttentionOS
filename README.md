# Attention OS v5.2

**个人注意力操作系统 — 从被动监控到主动引导**

Attention OS 是一款桌面端注意力管理工具。它通过截屏分析 + 本地活动信号融合，实时理解你的工作状态。v5.2 实现了核心转变：系统不再只是在你分心时提醒你，而是**主动告诉你此刻该做什么**，在检测到偏离时像朋友一样确认你的意图——是想休息一会儿，还是切回计划。

与普通效率工具不同，Attention OS 构建了一个完整的**计划 → 执行 → 回顾**闭环：

```
☀️ 早间 Briefing          🎯 专注时段           🌙 一日回顾
"今天想做什么？"    →    番茄钟 + 任务绑定    →    "对照早上的计划"
  设定今日目标            偏离时精准提醒           效率评分 + 反思
```

---

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 API 密钥（截屏分析需要 VL 模型）
cp .env.example .env
# 编辑 .env，填入 QWEN_API_KEY

# 3. 启动
python run.py
```

启动后自动打开浏览器访问 `http://127.0.0.1:5000`。

首次启动时会弹出**每日 Briefing**，引导你设定今日目标。

### 各平台说明

#### Windows（原生）
开箱即用，截图使用 `mss`，无需额外配置。

#### macOS
首次运行需授权：
- **屏幕录制**：系统偏好设置 → 隐私与安全性 → 屏幕录制 → 勾选 Terminal/IDE
- **辅助功能**：系统偏好设置 → 隐私与安全性 → 辅助功能 → 勾选 Terminal/IDE

#### Linux（原生桌面）
需要安装系统级截图工具：
```bash
sudo apt install scrot   # X11 环境
# 或
sudo apt install grim    # Wayland 环境
```

#### WSL2（Windows Subsystem for Linux）
WSL2 下 `mss`/PIL 的 `XGetImage()` 因 WSLg 虚拟显示层限制而无法使用。  
本项目已内置自动检测：**在 WSL2 下自动调用 `powershell.exe` 通过 Windows GDI+ 完成截图**，无需任何额外安装。

> 注意：请在 WSL2 终端中运行 `python run.py`，不要使用 Windows 侧的 Python 解释器。

---

## 核心功能

### ☀️ 每日 Briefing（`daily_briefing.py`）

每天打开应用时自动弹出，包含：
- **逾期任务警告** — 已过 deadline 的待办事项
- **今日到期提醒** — 今天 deadline 的任务
- **今日目标设定** — 输入 1-5 个今天最想完成的事（独立于 TodoList）

目标设定后，系统会持续追踪你的行为是否与目标一致。

### 🎯 任务感知提醒

当你连续偏离目标超过 10 分钟时，系统会根据场景生成不同优先级的提醒：

| 场景 | 提醒示例 |
|------|----------|
| 番茄钟专注中跑偏 | 🍅 你正在专注「写 SOP 文档」，还剩 12 分钟，坚持住！ |
| 有今日 deadline | 📋 你今天有任务要交付！当前目标「XX」还没完成 |
| 一般偏离 | 💡 你今早定的目标「XX」还在等你哦~ |

提醒之间有 15 分钟冷却期，不会过度打扰。

### 🍅 专注时段（`pomodoro.py`）

番茄钟升级为**专注时段**——开始专注前，可以从下拉菜单选择绑定的任务：
- 来自今日目标（Briefing 中设定的）
- 来自 TodoList 中的紧急/今日任务

专注完成后自动记录到**专注日志**，供一日回顾使用。

### 🌙 一日回顾

对照早间计划生成综合回顾：
- **综合评分**（0-100）— 目标完成率 × 40 + 生产率 × 40 + 番茄钟 × 20
- **目标逐项对照** — ✅/❌ 可视化
- **效率数据** — 生产率、分心率、开工时间
- **专注记录** — 每个番茄钟的任务名和时长
- **亮点与改进点** — 自动生成反思提示

### 📊 实时监控仪表盘

- 状态分布饼图（生产/分心/中性）
- 每小时效率曲线
- 活动率平滑曲线（5点移动平均）
- 本周趋势对比
- 每日开工时间柱状图（30天历史）

### 📋 智能 TodoList（`todo_manager.py`）

支持自然语言输入，AI 自动解析：
- 「明天下午前完成项目报告，紧急」→ 标题 + deadline + 优先级 + 标签
- 🎙 语音输入（Chrome/Edge Web Speech API）

### ⏰ 每小时签到（`hourly_checkin.py`）

每隔一小时弹窗询问「你在做什么」，收集第一视角的自我报告。晚间自动汇总生成叙事回顾。

### 🧠 智能回归提醒（`recovery_reminder.py`）

基于注意力恢复理论（Kaplan, 1995）和注意力残留效应研究（Mark et al., 2008），从三个维度追踪分心状态：神经递质恢复、注意力残留清除、任务上下文衰减。

---

## 与同类产品的对比

| 特性 | Attention OS | RescueTime | Forest | Toggl |
|------|-------------|------------|--------|-------|
| 截屏 AI 理解上下文 | ✅ VL 模型 | ❌ | ❌ | ❌ |
| 任务感知主动提醒 | ✅ LLM 动态生成 | ❌ | ❌ | ❌ |
| 计划→执行→回顾闭环 | ✅ | 部分 | ❌ | 部分 |
| 神经科学恢复模型 | ✅ 三维追踪 | ❌ | ❌ | ❌ |
| 本地部署/数据隐私 | ✅ | ❌ 云端 | ✅ | ❌ 云端 |
| 自然语言任务输入 | ✅ LLM | ❌ | ❌ | ❌ |
| 语音输入 + 情感识别 | ✅ SenseVoice | ❌ | ❌ | ❌ |
| 多模态 AI 整合 | ✅ 3个模型 | ❌ | ❌ | ❌ |

---

## 技术架构

### Multi-Agent 架构

本项目采用轻量级 Multi-Agent 架构，通过统一 LLM 调用层（`llm_client.py`）驱动多个专职 Agent：

| Agent | 职责 | 模型 |
|-------|------|------|
| Analyzer Agent | 截屏内容分析 | Qwen2.5-VL-72B-Instruct（视觉） |
| Coach Agent | 任务偏离提醒 | Qwen2.5-72B-Instruct（文本） |
| Reviewer Agent | 一日/周回顾 | Qwen2.5-72B-Instruct（文本） |
| Parser Agent | 自然语言任务解析 | Qwen2.5-72B-Instruct（文本） |
| Summarizer Agent | 签到叙事总结 | Qwen2.5-72B-Instruct（文本） |

### ModelScope 生态集成

| 模型 | 用途 | 调用方式 |
|------|------|----------|
| `Qwen/Qwen2.5-VL-72B-Instruct` | 截屏分析 | API Inference |
| `Qwen/Qwen2.5-72B-Instruct` | 文本生成（提醒/解析/回顾） | API Inference |
| `iic/SenseVoiceSmall` | 语音识别 + 情感检测 | 本地推理 / CPU |

---

## 系统要求

| 项目 | 要求 |
|------|------|
| Python | >= 3.9 |
| 操作系统 | macOS / Windows / Linux |
| VL 模型 API | Qwen2.5-VL-72B 或兼容接口 |

### 依赖概览

```
# 核心
requests, python-dotenv, Pillow, mss

# 活动监控
pynput, psutil

# Web 服务
fastapi, uvicorn, websockets

# 系统托盘
pystray

# macOS 特定
pyobjc-framework-Quartz, pyobjc-framework-Cocoa
```

---

## 项目结构

```
attention-os/
├── run.py                      # 入口：启动所有服务
├── main.py                     # 监控主循环（每 60 秒一个周期）
├── config.py                   # 全局配置
│
├── 🤖 AI 调用层
│   ├── llm_client.py           # 统一 LLM 调用客户端（文本 + 视觉）
│   ├── agents.py               # Multi-Agent 角色定义（Coach/Reviewer/Parser/...）
│   └── speech_recognition.py   # SenseVoice 语音识别 + 情感检测
│
├── 📊 数据采集层
│   ├── screenshot.py           # 屏幕截图
│   ├── analyzer.py             # VL 模型截屏分析（Analyzer Agent）
│   └── activity_monitor.py     # 键鼠活动 + 焦点窗口监控
│
├── 🧠 业务逻辑层
│   ├── state_fusion.py         # 多信号融合引擎
│   ├── daily_briefing.py       # Briefing + 任务感知提醒 + 一日回顾
│   ├── pomodoro.py             # 番茄钟（支持专注任务绑定 + 持久化）
│   ├── todo_manager.py         # TodoList + 自然语言解析（Parser Agent）
│   ├── hourly_checkin.py       # 每小时签到（Summarizer Agent）
│   ├── weekly_insight.py       # 周数据洞察（Reviewer Agent）
│   ├── daily_report.py         # 每日效率报告
│   ├── recovery_reminder.py    # 智能回归提醒（基于 ART 理论）
│   ├── work_start_tracker.py   # 每日开工时间追踪
│   └── break_reminder.py       # 定时休息提醒
│
├── 🖥 界面层
│   ├── web_server.py           # FastAPI Web 服务 + 全部 API
│   ├── static/index.html       # 单页前端（Chart.js + 雷达图）
│   ├── desktop_overlay.py      # 桌面悬浮窗 + 介入弹窗
│   ├── tray_app.py             # 系统托盘图标
│   ├── pomodoro_overlay.py     # 番茄钟浮窗
│   └── break_overlay_process.py # 全屏休息遮罩（子进程）
│
├── 💾 数据层
│   ├── database.py             # 监控记录存储与查询
│   └── data/                   # 所有持久化数据（JSON）
│       ├── work_logs.json
│       ├── todos.json
│       ├── daily_briefing.json
│       ├── focus_sessions.json  # 番茄钟专注记录（持久化）
│       ├── work_start_times.json
│       └── reports/
│
└── requirements.txt
```

---

## API 参考

### Briefing & 目标

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/briefing` | GET | 获取今日 Briefing 数据 |
| `/api/briefing/goals` | POST | 提交今日目标 |
| `/api/briefing/dismiss` | POST | 跳过 Briefing |
| `/api/briefing/goals/add` | POST | 追加目标 |
| `/api/briefing/goals/{i}/toggle` | POST | 切换目标完成 |
| `/api/briefing/goals/{i}/remove` | POST | 删除目标 |
| `/api/briefing/evening-review` | GET | 生成一日回顾 |
| `/api/briefing/nudge-status` | GET | 提醒状态 |

### 番茄钟

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/pomodoro/status` | GET | 当前状态 |
| `/api/pomodoro/start` | POST | 开始（body: `{focus_task, task_source}`） |
| `/api/pomodoro/pause` | POST | 暂停 |
| `/api/pomodoro/resume` | POST | 继续 |
| `/api/pomodoro/stop` | POST | 停止 |
| `/api/pomodoro/skip-break` | POST | 跳过休息 |

### 任务管理

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/todos` | GET/POST | 获取全部 / 添加任务 |
| `/api/todos/smart-add` | POST | 自然语言添加 |
| `/api/todos/{id}/toggle` | POST | 切换完成 |
| `/api/todos/{id}` | DELETE | 删除 |

### 监控数据

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/status` | GET | 实时状态 |
| `/api/today` | GET | 今日数据 |
| `/api/hourly` | GET | 每小时效率 |
| `/api/weekly` | GET | 7天趋势 |
| `/api/work-start/*` | GET | 开工时间 |
| `/api/report/*` | GET | 日报 |
| `/api/checkin/*` | - | 签到 |
| `/api/break/*` | - | 休息提醒 |
| `/ws` | WebSocket | 实时推送 |

### 语音识别（SenseVoice）

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/speech/transcribe` | POST | 语音转文字 + 情感识别（multipart/form-data） |
| `/api/speech/status` | GET | SenseVoice 模型状态 |

### 数据洞察

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/weekly-insight` | GET | 7天行为模式分析（LLM 驱动） |

---

## 版本历史

### v3.3（当前）
- ☀️ 每日 Briefing（开工时设定今日目标）
- 🎯 任务感知主动提醒（Coach Agent LLM 动态生成）
- 🍅 专注时段（番茄钟 + 任务绑定 + 专注日志持久化）
- 🌙 一日回顾闭环（Reviewer Agent + 五维雷达图）
- 🤖 Multi-Agent 架构（Coach / Reviewer / Parser / Summarizer）
- 🎙 SenseVoice 语音输入（替代 Web Speech API）
- 📊 周数据洞察（跨天行为模式分析）
- 🔧 统一 LLM 调用层（`llm_client.py`）
- 📦 全面切换至 Qwen 系列模型（3 个 ModelScope 模型）

### v3.2
- 默认首页切换为任务页
- 活动率移动平均平滑
- 每日开工时间追踪
- 分类逻辑修复（旦挞、浏览器、零活动度）

### v3.0
- 智能回归提醒、每日报告、番茄钟、TodoList、每小时签到

### v2.0
- 多信号融合引擎、活动监控、Web 仪表盘、休息提醒

### v1.0
- 基础截屏分析 + 状态记录
